meta {
  name: decrypt
  type: http
  seq: 4
}

get {
  url: {{url}}/decrypt/{{id}}/{{key}}
  body: none
  auth: inherit
}

script:pre-request {
  req.setMaxRedirects(0);
}

tests {
  test("307 Redirect", function () {
    expect(res.status).to.equal(307);
  })
}

settings {
  encodeUrl: true
}
